= Beavy Private Messaging

Allows users of the network to have private (group) conversations with each other.

== Configuration

Add ` private_messaging ` to your list of modules in the `$ROOT/config.yaml`

